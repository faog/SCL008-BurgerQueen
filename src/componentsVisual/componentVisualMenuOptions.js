import React, { Component } from 'react';

class ComponentVisualMenuOptions extends Component {
  render() {
    return (
      <div>Menu {this.props.menuname}</div>
    );
  }
}

export default ComponentVisualMenuOptions;
